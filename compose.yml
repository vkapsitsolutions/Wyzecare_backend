services:
  app:
    build: .
    ports:
      - '5000:5000'
    environment:
      - .env
    restart: always
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:5000/api/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
